public class ApuntesBuclesDibujos {
    //piramide si empezamos desde abajo
    /**
     * for (int i = 3; i >=0;i--{
     * int posicionI = (tamanio/2)-i;
     * 
     * 
     * for(j=posicionI;i < tamanio - posicionI;j--){
     * sout('*');
     * }
     * })
     * 
     */

     //piramide invertida
     /**
     * for(int i = 0;i < tamanio ; i++){
     *  for(j = i; j < tamanio - i - j;i++){
     *      sout('*');
     * }
     * }
     */
      /**
       * miFuncion(fechaNacimiento, FechaInicio, fechaFinal){
       * if(fechaInicio < fechaNacimiento < fechaFinal){
       * return ok;
       * }
       * }
       */
}
